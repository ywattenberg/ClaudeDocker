services:
  devbox:
    build: .
    hostname: devbox
    ports:
      - "2222:22"
    volumes:
      - ssh-host-keys:/etc/ssh/host_keys
      - dev-home:/home/dev
      - ~/.ssh/authorized_keys:/run/user-ssh/authorized_keys:ro

volumes:
  ssh-host-keys:
  dev-home:
